/**
 * Types for tRPC client
 */

// Import AppRouter type from API package
// This will be a placeholder until we can properly import from the API package
export type AppRouter = any;

export interface AuthToken {
  token: string;
  expiresAt: number;
}
